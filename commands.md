# Run commands

## Backend (from `backend/`, after `pip install -r requirements.txt` and `.env` set up)

```
uvicorn app.main:app --reload
```

## Frontend (from `frontend/`, after `npm install` and `.env` set up)

```
npm run dev
```
